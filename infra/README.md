# Phase 1 Infrastructure

This directory is reserved for shared infrastructure assets as the repository moves toward:

- `platform-java/platform-service` for the Java business platform
- `platform-java/gateway` for the public gateway and governance entrypoint
- `ai-service` for Python AI capabilities and embedding workflows
- `frontend` for the React application

Phase 1 keeps the root [`docker-compose.yml`](/Users/andrewhua/Desktop/Claude%20Project/project/智能工单JAVA/docker-compose.yml) as the primary development entrypoint while the service boundaries stabilize.
