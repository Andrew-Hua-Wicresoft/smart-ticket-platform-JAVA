spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      discovery:
        enabled: ${NACOS_DISCOVERY_ENABLED:false}
      config:
        enabled: ${NACOS_CONFIG_ENABLED:false}
    sentinel:
      enabled: ${SENTINEL_ENABLED:false}
    gateway:
      server:
        webflux:
          httpclient:
            connect-timeout: 3000
            response-timeout: ${GATEWAY_RESPONSE_TIMEOUT:45s}
          routes:
            - id: platform-service-v1
              uri: ${PLATFORM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/v1/**
              filters:
                - RewritePath=/api/v1/?(?<segment>.*), /api/${segment}
            - id: platform-service-legacy
              uri: ${PLATFORM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/**

server:
  port: 8080

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,gateway,metrics,prometheus

logging:
  level:
    org.springframework.cloud.gateway: INFO
  pattern:
    level: "%5p [request:%X{requestId:-}]"
